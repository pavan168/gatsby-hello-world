import React from "react"
import Layout from "../components/layout";

export default function Contact() {
    return (
        <Layout>
            <div style={{ color: `teal` }}>
                <p>Sample Gatsby page..</p>
            </div>
        </Layout>
    )
}