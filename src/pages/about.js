import React from "react";
import Layout from "../components/layout";

export default function About({ data }) {
    return (
        <Layout>
            <div style={{ color: 'teal' }}>
                <p>Uses a Layout component..</p>
            </div>
        </Layout>
    );
}